let a = 10; //prompt("Geef getal a:");
let b = 15; //prompt("Geef getal b:");

let resultaat = 0;

function telOp(x, y) {
    return +x + +y;
}
let som = telOp(a, b);

console.log("De som van " + a + " en " + b + " is " + som);
